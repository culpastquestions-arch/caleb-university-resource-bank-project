# Google Drive API Configuration
# Get these values by following the setup guide in docs/SETUP_DRIVE_API.md

# ⚠️ SECURITY NOTICE:
# These environment variables are used ONLY by the serverless backend function (api/drive.js)
# They are NEVER exposed to client browsers - your API key remains completely secure
# The backend proxy handles all Google Drive API calls server-side

# Your Google Cloud API Key
# Get this from: https://console.cloud.google.com/apis/credentials
# Used by the backend proxy to fetch data from Google Drive
GOOGLE_DRIVE_API_KEY=your_api_key_here

# The ID of your root Google Drive folder
# This is the folder containing all department folders
# Extract from the Drive URL: https://drive.google.com/drive/folders/YOUR_FOLDER_ID
GOOGLE_DRIVE_ROOT_FOLDER_ID=your_root_folder_id_here

# Cache duration in days (optional, defaults to 30)
CACHE_DURATION_DAYS=30

# Application Configuration
NODE_ENV=production

# ===== DEPLOYMENT INSTRUCTIONS =====
# 
# For Vercel:
# 1. Go to your project settings at https://vercel.com/dashboard
# 2. Navigate to "Settings" > "Environment Variables"
# 3. Add GOOGLE_DRIVE_API_KEY with your actual API key
# 4. Add GOOGLE_DRIVE_ROOT_FOLDER_ID with your folder ID
# 5. Make sure to set them for "Production", "Preview", and "Development" environments
# 6. Redeploy your application for changes to take effect
#
# The serverless function (api/drive.js) will automatically use these secure variables

